<!doctype html>
<html lang="en">
  {% include "partials/head.njk" %}
  <body>
    {% if not skipHeader %}
      {% include "partials/header.njk" %}
    {% endif %}
    {{ content | safe }}
    
    <meta name="robots" content="{% if process.env.NODE_ENV !== 'production' or draft %}noindex,nofollow{% else %}index,follow{% endif %}" data-helmet="robots" />
  </body>
</html>
