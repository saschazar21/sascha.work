{
  "name": "sascha.work",
  "version": "0.0.0-semantic-release",
  "private": true,
  "homepage": "https://sascha.work",
  "author": {
    "name": "Sascha Zarhuber",
    "url": "https://sascha.work"
  },
  "scripts": {
    "build": "rimraf out && npx @11ty/eleventy",
    "build:ci": "yarn build",
    "lighthouse": "npx lhci autorun",
    "lighthouse:all": "yarn build && npx lhci autorun",
    "lint:css": "stylelint **/*.scss",
    "lint:ts": "eslint --ext .js _data/ _assets/",
    "lint": "yarn lint:css && yarn lint:ts",
    "test:ci": "yarn --version",
    "test": "yarn test:ci && yarn lighthouse:all"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "@ampproject/eleventy-plugin-amp": "^0.5.3",
    "@lhci/cli": "^0.4.4",
    "eleventy-plugin-helmet": "^0.2.1",
    "eleventy-plugin-pwa": "^1.0.8",
    "eslint": "^6.8.0",
    "eslint-plugin-react": "^7.20.5",
    "html-minifier-terser": "^5.1.1",
    "nunjucks": "^3.2.2",
    "rimraf": "^3.0.2",
    "serve": "^11.3.2",
    "stylelint": "^13.6.1",
    "stylelint-config-recommended": "^3.0.0",
    "stylelint-config-styled-components": "^0.1.1",
    "stylelint-processor-styled-components": "^1.10.0"
  }
}
